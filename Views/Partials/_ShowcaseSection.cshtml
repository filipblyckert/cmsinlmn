@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage
@using Umbraco.Cms.Core.Models

 <section class="showcase">
                <div class="container">
                    <div class="content">
                        <h1>@Model.Value("showcaseTitle")</h1>
                        <p>@Model.Value("showcaseDescription")</p>
                        <a class="btn-yellow" href="@Model.Value("showcaseButtonUrl")">@Model.Value("showcaseButtonText")<i class="fa-regular fa-arrow-up-right"></i></a>
                        <a class="btn-transparent" href="services.html">Learn More <i class="fa-regular fa-arrow-up-right"></i></a>
                    </div>
                    
                    @{
                    var showcaseImage = Model.Value<MediaWithCrops>("showcaseImage");
                    if (showcaseImage != null)
                    {
                    <img src="@showcaseImage.MediaUrl()" alt="@Model.Value("showcaseTitle")">
                    }
                    
                    }
                    
                    
                </div>
        </section>